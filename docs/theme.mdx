import { Page, ThemeInfo } from './layout'

import {
  TachyonsTheme,
  GoTheme,
  BlankTheme
} from '../src/theme'

export default Page

export const name = 'Theme'

# Theme

## mdx-go

copy from [mdx-go](https://jxnblk.com/mdx-go/), just for simple theme for docments

<ThemeInfo theme={GoTheme} name='Mdx-go' />

## Tachyons

[Tachyons](https://tachyons.io/) doesn’t style component by default, but provides the necessary utils to generate a wide variety of component styles.

<ThemeInfo theme={TachyonsTheme} name='Tachyons' />

## Blank

Starter theme for implementing your design system

<ThemeInfo theme={BlankTheme} name='Blank' />

## Provider

**WIP**

ThemeProvider `withTheme`

## CreatTheme

**WIP**

Function to create new theme

## Choices

**WIP**

Choice

## Plugin

**WIP**

Plugins