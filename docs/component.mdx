import {
  Button,
  Heading,
  Link,
  Preset,
  Global,
} from '../src'
import { BlankTheme } from '../src/theme'
import { Page, ThemeInfo, ComponentDoc, PropsTable } from './layout'

export default Page

export const name = 'Component'

# Component

## Button

Extends - `Box`

```.jsx
<Button>Click</Button>
```

<PropsTable src={Button} />


## Link

Extends - `Box`


```.jsx
<Link href='#'>Link</Link>
```

<PropsTable src={Link} />

## Heading

Extends - `Text`


```.jsx
<Heading variant='h1'>Heading level 1</Heading>
<Heading variant='h2'>Heading level 2</Heading>
<Heading variant='h3'>Heading level 3</Heading>
<Heading variant='h4'>Heading level 4</Heading>
<Heading variant='h5'>Heading level 5</Heading>
```

<PropsTable src={Heading} />

## Preset

Preset is suppose to manage plain html tag styling in scoped div parent element. Useful for generated html snippets like markdown.

```.jsx
<Preset>
  <code>code</code>
  <pre>pre</pre>
  <hr />
</Preset>
```

<PropsTable src={Preset} />

## Global

Global stylings, pickup from `theme.global`

```jsx
<Global />
```

<ThemeInfo name='global' theme={BlankTheme.global}  collapsed={false} />
